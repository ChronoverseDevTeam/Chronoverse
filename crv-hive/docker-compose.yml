services:
  postgres:
    image: postgres:16
    container_name: crv-hive-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${CRV_POSTGRES_USER:-crv}
      POSTGRES_PASSWORD: ${CRV_POSTGRES_PASSWORD:-crv}
      POSTGRES_DB: ${CRV_POSTGRES_DB:-chronoverse_dev}
      POSTGRES_TEST_DB: ${CRV_POSTGRES_TEST_DB:-chronoverse_test}
    ports:
      - "${CRV_POSTGRES_PORT:-55432}:5432"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${CRV_POSTGRES_USER:-crv} -d ${CRV_POSTGRES_DB:-chronoverse_dev}",
        ]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 5s
    volumes:
      - crv-hive-postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro

volumes:
  crv-hive-postgres-data: